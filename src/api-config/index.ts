import apiClient, { config, authConfig } from "./Env";
import Network from "./Network";
import Urls from "./Urls";

export { apiClient, config, authConfig, Network, Urls };
export const api = apiClient;
